import { MedicationFormScreen } from "@/components/medications/medication-form-screen";

/** Add when opened bare; edit when opened with an `id` param. */
export default function MedicationForm() {
  return <MedicationFormScreen />;
}
